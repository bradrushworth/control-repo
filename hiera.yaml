---
version: 5

defaults:
  datadir: "data"

hierarchy:
  - name: 'Yaml backend'
    data_hash: yaml_data
    paths:
      - "passwords/%{trusted.certname}.yaml"
      - "nodes/%{trusted.certname}.yaml"
      - 'common.yaml'
